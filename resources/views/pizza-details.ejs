<div class="container mx-auto flex items-center justify-content-center mt-10">
    <div class="w-full md:w-64">
        <img class="h-40 mb-4 mx-auto" src="/img/<%= pizzaDetails.image %>" alt="">
        <div class="text-center">
            <h2 class="mb-4 text-lg"><%= pizzaDetails.name %></h2>
            <span class="size py-1 px-4 rounded-full uppercase text-xs"><%= pizzaDetails.size %></span>
            <div class="flex items-center justify-around mt-6">
                <span class="font-bold text-lg">₹<%= pizzaDetails.price %></span>
                <button data-pizza="<%= JSON.stringify(pizzaDetails) %>"
                    class="add-to-cart py-1 px-6 rounded-full flex items-center font-bold">
                    <span>+</span>
                    <span class="ml-4">Add</span>
                </button>
            </div>
        </div>
    </div>  
</div>
